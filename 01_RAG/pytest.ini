[pytest]
markers =
    slow: tests that require real API keys or external services
    es_integration: tests that use the local Elasticsearch service
    real_eval: tests that use the complete real RAG evaluation stack
filterwarnings =
    ignore:builtin type .* has no __module__ attribute:DeprecationWarning
